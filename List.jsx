import React from "react";
import { ReactDOM } from "react";

var number=[1,2,3,4,5];
const multiply = number.map((number) => {
    return multiply;
});
ReactDOM.render(
<ul>mutliply</ul>,
document.getElementsByID('app')
);
export default App;